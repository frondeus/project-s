No, it does not make sense.

With our inference we can only support splice as a last expression in the tuple.

Because think about it:

if we have a splice like this:

```
(1, 2, 3, ..a, ..b)
```

then we have no idea what is the size of `a`, so we can't restrict `b`...

```s
(let :three (tuple 3))
(let (:a: :b :c)
    (tuple 1 2 ..three)
)
```

```eval
val three : (3) = [
  3.0
]
val a: : 1 = 1.0
val b : 2 = 2.0
val c : 3 = 3.0
- : () = []
```

Okay, i can fix runtime later.
