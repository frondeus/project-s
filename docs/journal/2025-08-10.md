Okay, what about constructing new records


```s
(let :new (fn (:base) (do
    (let* :result (base result result))
    result
)))

```

thats basically fix right?


```s
(let :new (fn (:base) (do
    (let* :result (base result))
    result
)))

(new (fn (:self) {
    :a 1
    :b (thunk () (self :a))
}))
```

```eval auto-approve
val new : forall (('a) -?-> 'a) → 'b = "<Function: LispFn>"
- : {a: 1, b: 1} = {
  "a": 1.0,
  "b": 1.0
}
```

Aha, `..` operator together with such a `super` is not allowed. Makes sense tho

Okay, so this:

```s
(let :extend (fn (:base :ext) (do
    (let* :super (base super {})
          :result (ext result super)
    )
    result
)))
```
works

but this:

```s
(let :extend (fn (:base :ext) (do
    (let* :super (base super super)
          :result (ext result super)
    )
    result
)))
```

is not.

Because my dwarf is defined as:

```s
(let :dwarf (fn (:self :super) {
    :ancestry "Dwarf"
    :languages [ "Common" "Dwarvish" ]
    :features {
        :stout "Stout"
    }
    ..super
}))
```

Super must evaluate to `{}` here.

That is okay in my opinion tho.

One last thing

```s
(let :new (:
(fn ( (fn ('a {}) 'a) ) 'a )
(fn (:base) (do
    (let* :result (base result {}))
    result
))))

(new (fn (:self _) {
    :a 1
    :b (thunk () (self :a))
}))
```

Currently we MUST provide `_`.
Sure, we have `.._` operator in runtime
But when ascribing we cannot say "and any number of `{}`"
Can we?

```eval auto-approve
val new : (({a: 1, b: 1}, {}) → <'a ∧ 'b ∧ (:a) -?-> 'c> as 'a) → {a: 1, b: 1} = "<Function: LispFn>"
- : {a: 1, b: 1} = {
  "a": 1.0,
  "b": 1.0
}
```

No we dont. Ascribe.rs does not handle splices!

```s
(let :new (:
(fn ( (fn ('a ..{}) 'a) ) 'a )
(fn (:base) (do
    (let* :result (base result {}))
    result
))))

(new (fn (:self _) {
    :a 1
    :b (thunk () (self :a))
}))
```

```processed
(top-level
  (let
    :new
    (:
      (fn
        ((fn
            ((quote
                a)
              (splice
                (obj/plain)))
            (quote
              a)))
        (quote
          a))
      (fn
        (:base)
        (do
          (let*
            :result
            (base
              result
              (obj/plain)))
          result))))
  (new
    (fn
      (:self
        _)
      (obj/plain
        :a
        1
        :b
        (thunk
          (self)
          (self
            :a))))))
```

```eval auto-approve
Error: Unknown type: splice
   ╭─[ <input>:2:15 ]
   │
 2 │ (fn ( (fn ('a ..{}) 'a) ) 'a )
   │               ──┬─
   │                 ╰─── Used here
───╯

```
