Okay, fuck it, let's continue with implementing simple-sub

For now lets skip the simplification part.

One more case of lack of polymorphism and weird results:

```s
(let :outer (fn (:f) (do
    (let :res1 (f 5))
    (let :res2 (f "6"))
    f
)))

(outer (fn (:x) x))
```

```type
('a ∧ 'b) → 5 ∨ "6"
```

Mkay. For now i can live with that
