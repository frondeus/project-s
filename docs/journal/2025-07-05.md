I will start by introducing modules

```s

(let :id (fn (:f) f))

(module)
```

```type
module { }
```

For now we do not display content of the module because canocalization would need to support polymorphic schemes.

That means, this returs a monomorphic function (and id as well, but id is hidden from printing because its polymorphic).

```s
(let :id (fn (:x) x))
(let :foo (id (fn (:x) x)))

(module)
```

```type
module { foo: (Any) -> Any}
```

Note, technically we say its `(Any) -> Any` but it is not polymorphic!
It's just at this point it has `(?) -> ?` type, that will be further infered as soon as we call it.

Okay, now does it provide enough?

```s
(let :a (import "a.s"))

# ((a :foo) 4)

(a :foo)
```


```a.s
(let :foo (fn (:x) x))
(module)
```

```type
Error: Polymorphic module member: foo
   ╭─[ <input>:5:1 ]
   │
 5 │ (a :foo)
   │ ────┬───
   │     ╰───── Used here
   │
   ├─[ a.s:2:1 ]
   │
 2 │ (module)
   │ ────┬───
   │     ╰───── Module defined here
───╯

```

Shit. Current version of polymorphism is hard AF to use.
It's because we pass boxed function that needs certain environment (here, typeEnv).

It sucks, not gonna lie.

Maybe switching to Simple-Sub actually would make sense?
