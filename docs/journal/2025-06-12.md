Patryk had a question

```nix
nix-repl> fix = f: let result = f result; in result
nix-repl> pkgs = self: { a = 3; b = 4; c = self.a+self.b; }
nix-repl> fix pkgs
{ a = 3; b = 4; c = 7; }
```


```
(let :fix (fn (:f) (do
    (let* :result (f result))
    result
)))

(let :pkgs (fn (:this) { # self is a keyword, lets use `this` instead.
    :a 3
    :b 4
    :c (+ (this :a) (this :b))
}))

(fix pkgs)
```

```json
"<Error: Thunk is already evaluating>"
```

But do we need this fix?

```
{
    :a 3
    :b 4
    :c (+ (self :a) (self :b))
}
```

```json
Error: Undefined variable: self
   ╭─[ <input>:4:12 ]
   │
 4 │     :c (+ (self :a) (self :b))
   │            ──┬─  
   │              ╰─── Used here
───╯
Error: Undefined variable: self
   ╭─[ <input>:4:22 ]
   │
 4 │     :c (+ (self :a) (self :b))
   │                      ──┬─  
   │                        ╰─── Used here
───╯

```

# Let*

```
(let* :a (fn (:x) (do
    (print "a")
    (if (> x 0)
        (+ (a (- x 1)) 1)
        0
    )
)))

(a 3)
```

```json
3.0
```

```traces
 INFO project_s::runtime::s_std::functions: String("a")
 INFO project_s::runtime::s_std::functions: String("a")
 INFO project_s::runtime::s_std::functions: String("a")
 INFO project_s::runtime::s_std::functions: String("a")

```