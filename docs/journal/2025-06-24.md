```
(let :m (macro (:x) `(list ,x ,x)))

(m 10)
```

```macro
(do
  (let
    :m
    ())
  (list
    10
    10))
```

```
(let :m (macro (:x) '(list 1 2)))

(m 10)
```


```macro
(do
  (let
    :m
    ())
  (list
    1
    2))
```

Okay, so I was thinking about macros and how to deal with them in the context of type safety.

For now, i will stick to macro expansion and forbid using "dynamic" macros.
I do not have time to reinvent the wheel, and this is already blocking me from having any working language.
I need syntax rewriting macros for now at least. Maybe some day later i will add more metaocaml-like code-as-a-first-class-citizen macros.

```
(macro (:x) 1)
```

```macro
Error: Macro is forbidden in this context
   ╭─[ <input>:1:1 ]
   │
 1 │ (macro (:x) 1)
───╯

```

```
(let :m (+ 1 (macro (:x) `(list ,x ,x))))
```

````macro
Error: Macro is forbidden in this context
   ╭─[ <input>:1:14 ]
   │
 1 │ (let :m (+ 1 (macro (:x) `(list ,x ,x))))
───╯

````

```
(let :m (macro (:x) 1))

(m 10)
```

```macro
Error: Expected quote or unquote
   ╭─[ <input>:1:21 ]
   │
 1 │ (let :m (macro (:x) 1))
───╯

```

```
(let :m (macro (:x) (+ 2 3)))

(m 10)
```

```macro
Error: Expected quote or unquote
   ╭─[ <input>:1:21 ]
   │
 1 │ (let :m (macro (:x) (+ 2 3)))
───╯

```

# Recursive macros.

Okay. THAT works:

```
(let :m (macro (:x) `(list ,x ,x)))

(m 10)
```

```macro
(do
  (let
    :m
    ())
  (list
    10
    10))
```

What about:

```
(let :first (macro (:x) `(list ,x ,x)))
(let :second (macro (:x) `(first ,x)))

(second 10)
```

```macro
(do
  (let
    :first
    ())
  (let
    :second
    ())
  (list
    10
    10))
```

Great!

# Macro sanitization

Now the only missing part is to nuke `macro` invocation after the expansion pass

# Native macros

```
(let* :a 1)
```

```macro
(do
  (let-rec
    :a
    (thunk
      ()
      1)))
```

Holy!

```
{
   :a 1
   :b (+ 1 2)
}
```

```macro
(do
  (obj/con
    (fn
      (:self
        :root
        :super
        :origin)
      (do
        (obj/insert
          self
          :a
          (thunk
            (self
              root
              super
              origin)
            (obj/construct-or
              1)))
        (obj/insert
          self
          :b
          (thunk
            (self
              root
              super
              origin)
            (obj/construct-or
              (+
                1
                2))))
        self))))
```

# Fixing some issues

Okay, now  this seems to be broken:

```
(let :fundef (macro (:x) `(fn () ,x) ))

(let :fun (fn () (do
  (let :x 10 )
  (do
  (fundef x))
)))

(let :outer (fun))
(outer)
```

This macro expansion is not correct. Why `fundef` is not expanded?

```macro
(do
  (let
    :fundef
    ())
  (let
    :fun
    (fn
      ()
      (do
        (let
          :x
          10)
        (do
          (fn
            ()
            x)))))
  (let
    :outer
    (fun))
  (outer))
```

```json
10.0
```

Good.