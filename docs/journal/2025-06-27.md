## Recursive types

```
(let* :f (fn () f))
f
```

```type
() -> <recursive>
```

With type annotations:

```
(let* :f (: 
    (as 'a (fn () 'a))
    (fn () f)
))
f
```

```type
() -> () -> <recursive>
```

## Spans.

Okay so spans are heavy and hard to use.
Let's make them copyable