## Recursive types

```
(let* :f (fn () f))
f
```

```type
() -> <recursive>
```

With type annotations:

```
(let* :f (: 
    (as 'a (fn () 'a))
    (fn () f)
))
f
```

```type
() -> () -> <recursive>
```