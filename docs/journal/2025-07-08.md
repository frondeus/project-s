Okay, what's missing in simple type:

* [ ] Import
* [ ] Module
* [ ] Ascription
* [ ] Let*

Let's start with let*

Okay, does it work?
Let's try with classic example

```s
(let :fix (fn (:f) (do
    (let* :result (f result))
    result
)))

(let :mono (fn (:f) (do
    (let :obj (fn (:self) (obj/plain
        :a 1
        :b (thunk () (self 1))
    )))

    (let :res (f obj))
    (obj/plain
        :res res
        :f f
    )
)))

(mono fix)
```

Yeah, its broken.

```simple-type
{res: 'a, f: (('b) -?-> 'a) → 'b ∨ 'c}
```

```json
Error: Expected keyword literal
    ╭─[ <input>:9:22 ]
    │
  7 │ ╭─▶     (let :obj (fn (:self) (obj/plain
    ┆ ┆
  9 │ │           :b (thunk () (self 1))
    │ │                        ────┬───
    │ │                            ╰───── Used here
 10 │ ├─▶     )))
    │ │
    │ ╰───────────── Expected here
────╯

```

Simpler example

```s
(let*
    :even (fn (:x)
        (if
            (= x 0)
            true
            (odd (- x 1))
        )
    )
    :odd (fn (:x)
        (if
            (= x 0)
            false
            (even (- x 1))
        )
    )
)

even
```

```simple-type
('a ∧ number) → true ∨ false
```

```type
(number) -> true | false
```

Ah yes. We don't have definition of `-` or `=`!

Okay. Damn.
That doesn't look that bad tbh.
Sure `'a ^ number` is a bit excessive, but it is still true.

But im bit worried that the `:fix` example did not find the error.
