Do i need simplification of types?

lets say i have a function:

```

(let :f 
    (fn (:obj)
        (- (obj :a) (obj :b))
    )
)

f
```

What is the type of `:obj`?

```type
(TODO: UApplication { args: Value(33), ret: Use(28), field: (Some("a"), Use(32)), index: (None, Use(30)) } & TODO: UApplication { args: Value(41), ret: Use(36), field: (Some("b"), Use(40)), index: (None, Use(38)) }) -> number
```

It's because we can't guess that :obj is a record.
But what if we could?

What if adding ascription would help?

```

(let :f  (: (fn ((record)) :number)
    (fn (:obj)
        (- (obj :a) (obj :b))
    )
    )
)

f
```

It doesnt help because at that point we dont have row polymorphism.
We cannot express in the type ascription that we want `'a extends {}`

```type
Error: Undefined field: a
   ╭─[ <input>:4:12 ]
   │
 4 │         (- (obj :a) (obj :b))
   │            ────┬───  
   │                ╰───── Undefined field: a
───╯
Error: Undefined field: b
   ╭─[ <input>:4:21 ]
   │
 4 │         (- (obj :a) (obj :b))
   │                     ────┬───  
   │                         ╰───── Undefined field: b
───╯

```