Do i need simplification of types?

lets say i have a function:

```

(let :f
    (fn (:obj)
        (- (obj :a) (obj :b))
    )
)

f
```

What is the type of `:obj`?

```type
((:a) -?-> number ∧ (:b) -?-> number) → number
```

It's because we can't guess that :obj is a record.
But what if we could?

What if adding ascription would help?

```

(let :f  (: (fn ((extend :a :number :b :number)) :number)
    (fn (:obj)
        (- (obj :a) (obj :b))
    )
    )
)

(f (obj/plain
  :a 1
  :b 2
  :c 3
))

f
```

```type
('a extends {a: number, b: number}) → number
```
