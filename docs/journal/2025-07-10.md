Found it!
Funcking found it!
There was a disjoint between polymorphic type used in "let" and recursive monomorphic representation in letrec

```s
(let :fix (fn (:f) (do
    (let* :result (f result))
    result
)))

(let :obj (fn (:self) (obj/plain
    :a "2"
    :b (thunk () (self 1))
)))

(fix obj)
```

```simple-type
Error: expected a keyword literal
   ╭─[ <input>:8:18 ]
   │
 6 │ ╭─▶ (let :obj (fn (:self) (obj/plain
   ┆ ┆   
 8 │ │       :b (thunk () (self 1))
   │ │                    ────┬───  
   │ │                        ╰───── But here we have an application that is not asking about field
 9 │ ├─▶ )))
   │ │         
   │ ╰───────── This is a record
───╯

```

Bingo!

So now...

```s
(let :fix (fn (:f) (do
    (let* :result (f result))
    result
)))

(let :obj (fn (:self) (obj/plain
    :a "2"
    :b (thunk () (self :a))
)))

(fix obj)
```

```simple-type
{a: "2", b: "2"}
```

YES!

Okay, next big question (eh)

Why here:

```s
(let :f (fn (:x) x))

(f 4)

f
```

im not getting
```simple-type
('a) → 'a
```
