Rembember how we had to deal with unique values when printing types?

Well... We "solved" it by making sure only one value is added to the graph.
But that worked only when we did not take spans into account.

VTypeHead must be unique per span.
But at the same time when we print we must somehow distil the fact,
that

```
let x = 1
and y = 2
```
that x and y have the same type.

Additionally in the future it might be useful to have a way to for example "find all references" of given type.

Ok. So i added "type definition" to the core.
Its another node in the graph, and type values can point
to it.

```
(if true 1 2)
```

```type
1 ∨ 2
```

```graphviz
digraph G {
N0 [label="0 - lvl0 - Literal { value: Bool(true) }\n`true`"];
N1 [label="1 - lvl0 - Primitive { name: \"bool\" }"];
N2 [label="2 - lvl0 - Primitive { name: \"bool\" }"];
N3 [label="3 - lvl0 - Literal { value: Number(1.0) }\n`1`"];
N4 [label="4 - lvl0 - Primitive { name: \"number\" }"];
N5 [label="5 - lvl0 - Literal { value: Number(2.0) }\n`2`"];
N6 [label="6 - lvl0 - Primitive { name: \"number\" }"];
N7 [label="7 - lvl0 - Variable { id: VarId(0) }\n`(top-level (if true 1 2))`"];
START -> N7
N7 -> N3 [label="LO"];
N7 -> N5 [label="LO"];
N1 -> N0 [label="<:";color=red];
N2 -> N0 [label="<:";color=red];
N4 -> N3 [label="<:";color=red];
N6 -> N5 [label="<:";color=red];
N7 -> N3 [label="<:";color=red];
N7 -> N5 [label="<:";color=red];
}

```

```
4
```

```type
4
```

```
(let :x (if true 5))

x
```

```type
5 ∨ ()
```

```
(fn (:x) x)
```

```type
('a) → 'a
```

```
(fn (:x :y) (
    if x y
))
```

```type
(bool, 'a) → ()
```
