```s
(let :f (:
    (fn ((extend :a :number)) :number)
    (fn (:o)
        (o :a)
    )
))

(let :obj (obj/plain
    :a 1
    :b 2
))

(f obj)

f
```

```simple-type
('a extends {a: number}) → number
```

```s
(let :fix (:
    (fn ((fn ('r) 'r)) 'r)
    (fn (:f) (do
       (let* :result (f result))
       result
    ))
))

fix
```

```simple-type
(('a) → <'a ∧ 'a> as 'a) → 'a
```

Yeah i think its another thing we could simplify...

I think its time to switch from old typer to new one.
