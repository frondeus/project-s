I have a suspicion of where the problem might be.

I think i have broken polarity of Application type.
In  original whitepaper there was no application type. Instead there was a function type used
as a bound. That made sense when you can distinguish between function calls, indexing tuples and lists and accessing the field of the record.

```s
(let :fix (fn (:f) (do
    (let* :result (f result))
    result
)))

fix
```

```simple-type
(('a) -?-> 'a) → 'b
```

```type
(('a) -?-> 'a) → 'b
```
