/Users/frondeus/Code/project-s/docs/journal/2025-06-09.md:73
````
(let :fundef (macro (:x) `(fn () ,x) ))

(let :fun (fn () (do
  (let :x 10 )
  (do
  (fundef x))
)))

(let :outer (fun))
(outer)
````

--- /Users/frondeus/Code/project-s/docs/journal/2025-06-09.md
+++ json-004
@@ -83,9 +83,19 @@
 (outer)
 ```
 
-```json
-10.0
-```
+````json
+Error: Expected quote or unquote
+   ╭─[ <input>:1:35 ]
+   │
+ 1 │ (let :fundef (macro (:x) `(fn () ,x) ))
+───╯
+Error: Expected int literal to access tuple element
+   ╭─[ :1:1 ]
+   │
+ 1 │ 
+───╯
+
+````
 
 Okay. That works now. But there is another problem
 
