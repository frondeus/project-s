I have veeery weird case.

```s
(let :id (fn (:x) x))
(let :fix-super (fn (:f :super) (do (let* :result (f result super)) result )))
(let :fix-record (fn (:f) (do (let* :result (f result (obj/plain))) result )))
(let :extend (fn (:base :ext) (do (fix-super ext base) )))
(let :to-record (fn (:plain) (fn (:self :super) plain) ))
(let :extend-fn (fn (:base :ext) (do (to-record (extend base ext)) )))

(let :first (obj/plain
    :a 1
))

(let :second (id (obj/record
    :a (+ (super :a) 1)
    :b 2
)))


(let :result (extend first second))
result
```

```type ignore
{a: 1, a: number, b: 2}
```

Okay...

Now lets extend it

```s
(let :id (fn (:x) x))
(let :fix-super (fn (:f :super) (do (let* :result (f result super)) result )))
(let :fix-record (fn (:f) (do (let* :result (f result (obj/plain))) result )))
(let :extend (fn (:base :ext) (do (fix-super ext base) )))
(let :to-record (fn (:plain) (fn (:self :super) plain) ))
(let :extend-fn (fn (:base :ext) (do (to-record (extend base ext)) )))

(let :first (obj/plain
    :a 1
))

(let :second (id (obj/record
    :a (+ (super :a) 1)
    :b 2
)))

(let :third (id (obj/record
    :a (+ (super :a) 2)
    :b (+ (super :b) 1)
)))


(let :result (extend (extend first second) third))
result
```

Ooooooh
Why json is returning error but type is not?

```json ignore
{
  "a": 4.0,
  "b": 3.0
}
```

```type ignore
{a: 1, a: number, b: 2, a: number, b: number}
```

??? How does it work?

The only difference I see is lambda lifting...

Yeah it was value restriction algorithm that did not
recognize that `cl` is a function (a closure).

One question, are our polymorphic fns broken in a way?


```s
(let :f (fn (:x) (do
    (+ "1" 2)
    x
))
```

```type
Error: Type mismatch
   ╭─[ <builtin>:1:7 ]
   │
 1 │ "+": (number, number, ) -> number
   │       ───┬──  
   │          ╰──── Expected number
   │
   ├─[ <input>:2:8 ]
   │
 2 │     (+ "1" 2)
   │        ─┬─  
   │         ╰─── But found "1"
───╯

```

Yeah, we dont immediately check f
Okay now we do :)

# Lists

Okay so i added couple of builting methods to deal with lists


```s
(let :s (list/enumerate (list 1 2 3)))
s
```

```type
[(number, 1 ∨ 2 ∨ 3)]
```

```json
[
  [
    0.0,
    1.0
  ],
  [
    1.0,
    2.0
  ],
  [
    2.0,
    3.0
  ]
]
```

Great!

```s
(let :s (list/map (list 2 3 4) (fn (:x) (- x 1) )))
s
```

```type
[number]
```

```json
[
  1.0,
  2.0,
  3.0
]
```

Great!


```s
(let :s (list/find-or
    (list 1 2 4)
    (fn (:x) (= x 2))
    0
    )
)
s
```

```type
4 | 2 | 1 | 0
```

```json
2.0
```

```s
(let :s (list/find-or
    (list 1 2 4)
    (fn (:x) (= x 5))
    0
    )
)
s
```

```type
4 | 2 | 1 | 0
```

```json
0.0
```

Ok big shot:

```s
(let :mod (fn (:stat) (do
    (let :stats [ 3 5 7 9 11 13 15 17 ])
    (let (:result :val) (list/find-or
        (list/map
            (list/enumerate stats)
            (fn ((:idx :val)) (tuple (- idx 4) val))
        )
        (fn ((:idx :val)) (<= stat val))
        (tuple 4 18)
        )
    )
    result
)))
(mod 12)
```

```json
1.0
```

Ugh, almost...

Lets break it into smaller pieces

```s
(let :stat 12)
(let :stats [ 3 5 7 9 11 13 15 17 ])
(let :enumerated-stats (list/enumerate stats ))
(let :mapped-stats (list/map
    enumerated-stats
    (fn ((:idx :val)) (tuple (- idx 4) val))
))
(let :found-stats (list/find-or
    mapped-stats
    (fn ((:idx :el)) (<= stat el))
    (tuple 4 18)
))
found-stats
```

```json
[
  1.0,
  13.0
]
```

Huh?
```s
(let :a 1)
(let :b 2)
(<= a b)
```

```json
true
```

# Adding two records vs super

One thing regressed a bit.
Can I express that kind of operation?


```s
(let :fix-record  (fn (:f) (do
    (let* :result (f result (obj/plain)))
    result
)))

(let :a (obj/record
    :x 1
    :y (thunk () (self :x 1))
))
(fix-record a)
```

```json ignore
{
  "x": 1.0,
  "y": 1.0
}
```
Okay that works. But when extending...

```s
(let :fix (fn (:f) (do (let* :result (f result)) result )))
(let :fix-super (fn (:f :super) (do (let* :result (f result super)) result )))
(let :fix-record  (fn (:f) (do (let* :result (f result (obj/plain))) result )))
(let :extend (fn (:base :ext) (do
    (fix-super ext (fix base))
)))
(let :a (fn (:self) (obj/plain
    :x 1
    :y (thunk () (self :x 1))
)))
(let :b (obj/record
    :x 2
))
(extend a b)
```

```json ignore
{
  "x": 2.0,
  "y": 1.0
}
```

Yeah, "y" stayed the same.
Can I make it work?

I think what i need is mutually recursive let.

```s
(let*
    :even (fn (:x)
        (if
            (= x 0)
            true
            (odd (- x 1))
        )
    )
    :odd (fn (:x)
        (if
            (= x 0)
            false
            (even (- x 1))
        )
    )
)

(even 7)
```

```json
false
```

Cool. But i managed to break something:

```s
(let :fix
    (fn (:f) (do
        (let* :result (f result))
        result
    ))
)

(fix (fn (:self)
    (obj/plain
    :x 1
    :y (thunk () (self :x))
    )
))
```

```processed ignore
(do
  (let :fix
    (fn (:f)
      (do
        (let-rec :result
          (thunk () (f result)))
        (debug result)
        result)))
  (fix
    (fn
      (:self)
      (obj/plain
        :x
        1
        :y
        (thunk
          (self)
          (self
            :x))))))
```

```type
{x: 1, y: 1}
```

Why the heck?

```json
{
  "x": 1.0,
  "y": 1.0
}
```

```traces

```

Wait, why i was using double thunk insertion??
Somehow just `let-rec` works without extra thunk...

Okay so there is no point of having `let*` as a macro call.

Great, now can i use this new mutually recursive let to expand self in a way i want?

```s

(let :expand (fn (:base :ext) (do
    (let*
        :super  (base result)
        :result (ext result super)
    )
    result
)))

(let :left (fn (:self) (obj/plain
    :x 1
    :y (thunk () (self :x))
)))

(let :right (fn (:self :super) (obj/extend super
    :x 2
)))

(expand left right)
```

```json ignore
{
  "x": 2.0,
  "y": 2.0
}
```

Holy crap that actually worked!
