I have veeery weird case.

```s
(let :id (fn (:x) x))
(let :fix-super (fn (:f :super) (do (let* :result (f result super)) result )))
(let :fix-record (fn (:f) (do (let* :result (f result (obj/plain))) result )))
(let :extend (fn (:base :ext) (do (fix-super ext base) )))
(let :to-record (fn (:plain) (fn (:self :super) plain) ))
(let :extend-fn (fn (:base :ext) (do (to-record (extend base ext)) )))

(let :first (obj/plain
    :a 1
))

(let :second (id (obj/record
    :a (+ (super :a) 1)
    :b 2
)))


(let :result (extend first second))
result
```

```type
{a: 1, a: number, b: 2}
```

Okay...

Now lets extend it

```s
(let :id (fn (:x) x))
(let :fix-super (fn (:f :super) (do (let* :result (f result super)) result )))
(let :fix-record (fn (:f) (do (let* :result (f result (obj/plain))) result )))
(let :extend (fn (:base :ext) (do (fix-super ext base) )))
(let :to-record (fn (:plain) (fn (:self :super) plain) ))
(let :extend-fn (fn (:base :ext) (do (to-record (extend base ext)) )))

(let :first (obj/plain
    :a 1
))

(let :second (id (obj/record
    :a (+ (super :a) 1)
    :b 2
)))

(let :third (id (obj/record
    :a (+ (super :a) 2)
    :b (+ (super :b) 1)
)))


(let :result (extend (extend first second) third))
result
```

Ooooooh
Why json is returning error but type is not?

```json
{
  "a": 4.0,
  "b": 3.0
}
```

```type
{a: 1, a: number, b: 2, a: number, b: number}
```

??? How does it work?

The only difference I see is lambda lifting...

Yeah it was value restriction algorithm that did not
recognize that `cl` is a function (a closure).

One question, are our polymorphic fns broken in a way?


```s
(let :f (fn (:x) (do
    (+ "1" 2)
    x
))
```

```type
Error: Incompatible types
   ╭─[ <builtin>:1:7 ]
   │
 1 │ "+": [number] -> number
   │       ───┬──  
   │          ╰──── Expected number
   │
   ├─[ <input>:2:8 ]
   │
 2 │     (+ "1" 2)
   │        ─┬─  
   │         ╰─── But got "1"
───╯

```

Yeah, we dont immediately check f
Okay now we do :)
