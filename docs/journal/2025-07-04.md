> Yeah, a function becomes monomorphic.

> Okay so probably the best way of fixing it so do > what Ocaml is doing.
> This article: https://okmij.org/ftp/ML/generalization.html#levels

Okay so that wont work.
Levels are one thing, but monomorphization is another.

To fully solve it i need to do:
* [ ] Introduce level checking to fix unsoundness of the let-polymorphism
in some cases.
* [ ] Create a new type `Module` that can be used to represent modules
  * [ ] It would have BTreeMap<String, Scheme> in contrast to `BTreeMap<String, Value>` in the record type.
  * [ ] It would also be a subtype of `Record` with behaviour that module used as an object is immediately instantiated (`BTreeMap<String, Scheme>` becomes `BTreeMap<String, Value>`).
* [ ] Fix value restriction checking
  * [ ] Allow the `import` keyword.
  * [ ] Allow tuples and redords as safe for restriction.


I think that should help.
