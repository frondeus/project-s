So now with literal trait i can express things like

```s
(let :only-five (:
    (fn (5) :number)
    (fn (:x) (- x 2))
))

(only-five 5)
```

This is accepted

```json
3.0
```


```s
(let :only-five (:
    (fn (5) :number)
    (fn (:x) (- x 2))
))

(only-five 6)
```

But this returns type error!

```json
Error: Incompatible types
   ╭─[ <input>:2:10 ]
   │
 2 │     (fn (5) :number)
   │          ┬  
   │          ╰── Expected 5
   │ 
 6 │ (only-five 6)
   │            ┬  
   │            ╰── But got 6
───╯

```

Especially if its not a literal:


```s
(let :only-five (:
    (fn (5) :number)
    (fn (:x) (- x 2))
))

(let :any-num (:
    (fn (:number) :number)
    (fn (:x) (only-five x))
))

(any-num 6)
```

```json
Error: Incompatible types
   ╭─[ <input>:2:10 ]
   │
 2 │     (fn (5) :number)
   │          ┬  
   │          ╰── Expected 5
   │ 
 7 │     (fn (:number) :number)
   │          ───┬───  
   │             ╰───── But got number
───╯

```

```s
(let :x (if true "yes" "no"))
x
```

```type
"yes" | "no"
```

```s
(- "a" 5)
```

```json
Error: Incompatible types
   ╭─[ <builtin>:2:7 ]
   │
 2 │ "-": [number] -> number
   │       ───┬──  
   │          ╰──── Expected number
   │
   ├─[ <input>:1:4 ]
   │
 1 │ (- "a" 5)
   │    ─┬─  
   │     ╰─── But got "a"
───╯

```

One annoying thing is the lack of `<builtin>` source.
I think i can generate that virtual string..

# Diagnostics in LSP

I think it makes sense to enable diagnostics in LSP.
