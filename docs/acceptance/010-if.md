For now if expr is only inside of object construction

```
{
  (if true '(:key 42))
  (if false '(:false 13))
  (if false '(:true 10) '(:else 12))
}
```

```cst
List([Symbol("do"), List([Symbol("struct"), List([Symbol("if"), Bool(true), List([Symbol("quote"), List([Keyword("key"), Number(42.0)])])]), List([Symbol("if"), Bool(false), List([Symbol("quote"), List([Keyword("false"), Number(13.0)])])]), List([Symbol("if"), Bool(false), List([Symbol("quote"), List([Keyword("true"), Number(10.0)])]), List([Symbol("quote"), List([Keyword("else"), Number(12.0)])])])])])
```

```json
{
  "else": 12.0,
  "key": 42.0
}
```
