For now if expr is only inside of object construction

```
{
  (if true '(:key 42))
  (if false '(:false 13))
  (if false '(:true 10) '(:else 12))
}
```

```cst
List([Symbol("do"), List([Symbol("obj/struct"), List([Symbol("if"), Bool(true), List([Symbol("quote"), List([Keyword("key"), Number(42.0)])])]), List([Symbol("if"), Bool(false), List([Symbol("quote"), List([Keyword("false"), Number(13.0)])])]), List([Symbol("if"), Bool(false), List([Symbol("quote"), List([Keyword("true"), Number(10.0)])]), List([Symbol("quote"), List([Keyword("else"), Number(12.0)])])])])])
```

```json-eager
{
  "else": 12.0,
  "key": 42.0
}
```

# Ifs in outside


```
(if true 4)
```

```json
4.0
```

Note, we are not forcing directly the else branch.
Instead we raise an error.
That's how we can still use one-branch-if if we just need side effects.

```
(if false 4)
```

```json
"<Error: No else branch>"
```

```
(if false 4 5)
```

```json
5.0
```

Ifs evaluate branch lazily.
else branch is never evaluated here

```
(if true 4 
  (print "false!")
)
```

```log

```