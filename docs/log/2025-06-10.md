So we want to have a thunk but only if a value is not a constructor?

So the root cause is:
```example
(obj/put :a (thunk (self root origin) (obj/con (...)) ))
```

{ a: thunk }
now we call obj.a

thunk is evaluated - we create a constructor
But we do not immediately create constructed object!

What if i had

```
(let :x {:e 1 :f (root :d)})

{
  :a (obj/construct-or {
    :b 1
    :c (root :d)
  })
  :d (obj/construct-or 2)
  :g (obj/construct-or x)
}

```

```json 
{
  "a": {
    "b": 1.0,
    "c": 2.0
  },
  "d": 2.0,
  "g": {
    "e": 1.0,
    "f": 2.0
  }
}
```

Okay, this works, so we could use (obj/construct-or) here, right?

Ok, let's try it:

```
(let :x {:e 1 :f (root :d)})

{
  :a {
    :b 1
    :c (root :d)
  }
  :d 2
  :g x
}
```

```json
{
  "a": {
    "b": 1.0,
    "c": 2.0
  },
  "d": 2.0,
  "g": {
    "e": 1.0,
    "f": 2.0
  }
}
```

Okay... Now we got suprising behaviour of root...

```
(+ {
  :another 4
  :key {
    :b (+ 1 (root :another))
  }
}

{
  :another 9
})
```

for some reason `root` is pointing into `another` from right side!
I mean... maybe it is what we want?

```json
{
  "another": 9.0,
  "key": {
    "b": 10.0
  }
}
```

So... Javascript version of this returns 5.

Whether i want it or not:
I think i do want it!

Imagine that the right side is a config provided by the user
while left side is something there already.
By overriding `another` we can automatically recalculate `key.b`.

Sure, it leads to more unexpected behaviour but in the context of rpg and
character sheets, it might be worth it.

One example - changing level of the character. Very often max HP is based
on the current level. 

Okay, but now the only question is: **WHY** is it working like this?

```traces debug ignore
DEBUG project_s::runtime: Expanded: (obj/condef (obj/put :another (thunk (self root origin) (obj/construct-or 4))) (obj/put :key (thunk (self root origin) (obj/construct-or (obj/struct :b (+ 1 (root :another)))))))
DEBUG project_s::runtime: Expanded: (obj/con (fn (:self :root :origin) (do (obj/put :another (thunk (self root origin) (obj/construct-or 4))) (obj/put :key (thunk (origin root self) (obj/construct-or (obj/struct :b (+ 1 (root :another)))))) self)))
DEBUG project_s::runtime: Expanded: (obj/condef (obj/put :another (thunk (self root origin) (obj/construct-or 9))))
DEBUG project_s::runtime: Expanded: (obj/con (fn (:self :root :origin) (do (obj/put :another (thunk (self root origin) (obj/construct-or 9))) self)))
DEBUG project_s::runtime::structs: Constructor call: None
DEBUG add_obj_impl: project_s::runtime::s_std: Adding obj: Constructor(Constructor { constructor: LispFn }), Constructor(Constructor { constructor: LispFn })
DEBUG add_obj_impl: project_s::runtime::s_std: Self: Ref(RefCell { value: Object({}) })
DEBUG add_obj_impl: project_s::runtime::s_std: Root: Ref(RefCell { value: Object({}) })
DEBUG add_obj_impl: project_s::runtime::structs: Constructor call: Some(Ref(RefCell { value: Object({}) }))
DEBUG add_obj_impl: project_s::runtime::structs: (do (obj/put :another (thunk (self root origin) (obj/construct-or 4))) (obj/put :key (thunk (origin root self) (obj/construct-or (obj/struct :b (+ 1 (root :another)))))) self)
DEBUG add_obj_impl: project_s::runtime: Expanded: (obj/insert self :another (thunk (self root origin) (obj/construct-or 4)))
DEBUG add_obj_impl:insert_to_struct: project_s::runtime::s_std: Inserting to struct({}): another - Thunk(Thunk)
DEBUG add_obj_impl:insert_to_struct: project_s::runtime::s_std: After insertion: {"another": Thunk(Thunk)}
DEBUG add_obj_impl: project_s::runtime: Expanded: (obj/insert self :key (thunk (origin root self) (obj/construct-or (obj/struct :b (+ 1 (root :another))))))
DEBUG add_obj_impl:insert_to_struct: project_s::runtime::s_std: Inserting to struct({"another": Thunk(Thunk)}): key - Thunk(Thunk)
DEBUG add_obj_impl:insert_to_struct: project_s::runtime::s_std: After insertion: {"another": Thunk(Thunk), "key": Thunk(Thunk)}
DEBUG add_obj_impl: project_s::runtime::structs: Created object: Ref(RefCell { value: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)}) })
DEBUG add_obj_impl: project_s::runtime::s_std: Left: Ref(RefCell { value: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)}) })
DEBUG add_obj_impl: project_s::runtime::s_std: Super: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)})
DEBUG add_obj_impl: project_s::runtime::s_std: Self: Ref(RefCell { value: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)}) })
DEBUG add_obj_impl: project_s::runtime::structs: Constructor call: Some(Ref(RefCell { value: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)}) }))
DEBUG add_obj_impl: project_s::runtime::structs: (do (obj/put :another (thunk (self root origin) (obj/construct-or 9))) self)
DEBUG add_obj_impl: project_s::runtime: Expanded: (obj/insert self :another (thunk (self root origin) (obj/construct-or 9)))
DEBUG add_obj_impl:insert_to_struct: project_s::runtime::s_std: Inserting to struct({"another": Thunk(Thunk), "key": Thunk(Thunk)}): another - Thunk(Thunk)
DEBUG add_obj_impl:insert_to_struct: project_s::runtime::s_std: After insertion: {"another": Thunk(Thunk), "key": Thunk(Thunk)}
DEBUG add_obj_impl: project_s::runtime::structs: Created object: Ref(RefCell { value: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)}) })
DEBUG project_s::runtime::structs: Created object: Ref(RefCell { value: Object({"another": Thunk(Thunk), "key": Thunk(Thunk)}) })
DEBUG project_s::runtime: Expanded: (obj/condef (obj/put :b (thunk (self root origin) (obj/construct-or (+ 1 (root :another))))))
DEBUG project_s::runtime: Expanded: (obj/con (fn (:self :root :origin) (do (obj/put :b (thunk (origin root self) (obj/construct-or (+ 1 (root :another))))) self)))
DEBUG project_s::runtime::structs: Constructor call: None
DEBUG project_s::runtime::structs: (do (obj/put :b (thunk (origin root self) (obj/construct-or (+ 1 (root :another))))) self)
DEBUG project_s::runtime: Expanded: (obj/insert self :b (thunk (origin root self) (obj/construct-or (+ 1 (root :another)))))
DEBUG insert_to_struct: project_s::runtime::s_std: Inserting to struct({}): b - Thunk(Thunk)
DEBUG insert_to_struct: project_s::runtime::s_std: After insertion: {"b": Thunk(Thunk)}
DEBUG project_s::runtime::structs: Created object: Ref(RefCell { value: Object({"b": Thunk(Thunk)}) })

```

Oh, okay. When adding two structs, we pass `root` into left side as `root`.
But `root` initially is not provided, so `self` is taken.
And we do mutate `self`.
That, including the fact that `:key` is a thunk, leads to the following - 
when we evaluate `:key`, we already added `:another` to `self`, so its `9`, not `4`, so `:b` is `10`!

#

Now fixing those last three cases.

```
(let :base {
  :a 5.0
})

(let :x {
  :key 42.0
  :value (+ base {
    :a (+ (super :a) 10.0)
  })
})

(+ {:d 1} x)
```


```json 
{
  "d": 1.0,
  "key": 42.0,
  "value": {
    "a": 15.0
  }
}
```

