Okay, so there are two things missing from the error messgaes.
First, we cannot treat error as a Result in typechecker.
We must maintain a list and continue to infer after an error.

Second, we need to print collected diagnostics.

Second part we can do with https://github.com/zesterer/ariadne

