I added basic LSP support.
For now that LSP only supports semantic highlighting.

It is because i did not want to write another grammar for the sake of VSCode,
but LSP can provide slower but still a highlighting.

So LSP is called, it asks tree sitter for highlights, and i transform
it to semantic tokens.
Quite basic idea and its working!