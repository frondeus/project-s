```
(let :m (macro (:x) `(list ,x ,x)))

(m 10)
```

```macro
(do (let :m (macro (:x) (quasiquote (list (unquote x) (unquote x))))) (m 10))
```

Okay, so I was thinking about macros and how to deal with them in the context of type safety.

For now, i will stick to macro expansion and forbid using "dynamic" macros.
I do not have time to reinvent the wheel, and this is already blocking me from having any working language.
I need syntax rewriting macros for now at least. Maybe some day later i will add more metaocaml-like code-as-a-first-class-citizen macros.

