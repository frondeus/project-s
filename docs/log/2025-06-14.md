It's been full month huh.

Next step before i finally start working on the type system is
higher level rust API for defining native functions and macros.
Something bevy like.

Okay, i added a new API, but there is one downside.
Currently, we are not handling errors the same way we did before.


```
(- 1 "Foo")
```


```json
"<Error: Expected number, got String(\"Foo\")>"
```

This is handled, but this:

```
(- 1 (error "Foo"))
```

```json
"<Error: Foo>"
```

This error handling is eager. Will this be a problem?


Anyway, lets live with it for now.

More work is needed tho - let's look at `+` operator.
It is overloaded - both numbers and objects can be used.
That itself is not a problem, but it becomes one with inconvenient
match expression.

Ideally i would like to introduce overloaded functions (only native ones)
where I would first match attributes, and call first function that matches the
signature.

Okay lets see:

```
(+ "Foo" "Bar")
```

```json
"<Error: +: Calling with no matching signature: [String(\"Foo\"), String(\"Bar\")]>"
```

It will have to be sufficient for now.

At this point, only static typing is left